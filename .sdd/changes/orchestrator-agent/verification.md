# Verification: orchestrator-agent

> Generated by `sdd-verify` on 2026-06-29.
> Verdict: **BLOCKED**

## Summary

- Tasks completadas: 14/15
- Requirements cubiertos: 6/7
- Tests ejecutados: 18 passed, 0 failed, 0 skipped
- Build/package: `mvn package -DskipTests` OK
- Docker: `docker compose config --quiet` OK; `docker compose build orchestrator-agent` OK desde WSL; `docker compose up -d orchestrator-agent` OK
- Smoke tests: bypass invalido OK (400); bypass a downstream FALLA (502)
- Findings: 2 CRITICAL / 2 WARNING / 1 SUGGESTION

## Evidence

- Shell detectado: PowerShell. Docker runtime operativo desde WSL/Ubuntu.
- `mvn test` en `agents/orchestrator-agent`: 18 tests, 0 failures, 0 errors, 0 skipped.
- `mvn package -DskipTests`: BUILD SUCCESS y JAR generado.
- `docker compose config --quiet`: sin errores.
- `wsl -d Ubuntu -- docker compose build orchestrator-agent`: imagen `genai-stack-orchestrator-agent:latest` construida.
- `wsl -d Ubuntu -- docker compose up -d orchestrator-agent`: container `orchestrator-agent` started y puerto `8083` publicado.
- `POST /api/v1/ask` con `X-Bypass-Routing: unknown`: HTTP 400 con `{"error":"BAD_REQUEST",...}`.
- `POST /api/v1/ask` con `X-Bypass-Routing: doc-query`: HTTP 502.
- `POST /api/v1/ask` con `X-Bypass-Routing: diagnosis`: HTTP 502.
- Logs del orchestrator: `Downstream agent failed: doc-query-agent request failed` y `diagnosis-agent request failed`.

## Spec Compliance Matrix

| Requirement | Scenario | Test | Status |
|-------------|----------|------|--------|
| Contrato de API | pregunta valida | `OrchestratorControllerTest.shouldReturnAnswerAndRoutedToWhenRequestIsValid()` | COMPLIANT (unit) |
| Contrato de API | question vacia o ausente | `OrchestratorControllerTest.shouldReturnBadRequestWhenQuestionIsBlank()` | COMPLIANT |
| Ruteo via LLM local | pregunta sobre documentacion | `SpringAiOrchestrationAdapterTest.shouldReturnFirstRoutedAgentWhenToolWasInvoked()` | PARTIAL (mocked only) |
| Ruteo via LLM local | pregunta con stacktrace | no E2E passing | UNTESTED |
| Ruteo via LLM local | pregunta general | no E2E passing | UNTESTED |
| Descripcion de tools | delegacion a doc-query-agent | `AgentToolsTest.shouldCallDocQueryAgentAndTrackRoute()` | COMPLIANT (unit) |
| Descripcion de tools | diagnosis con logContent | `AgentToolsTest.shouldCallDiagnosisAgentWithLogPayloadAndFlattenResponse()` | COMPLIANT (unit) |
| Campo routedTo | una tool invocada | `SpringAiOrchestrationAdapterTest.shouldReturnFirstRoutedAgentWhenToolWasInvoked()` | COMPLIANT |
| Campo routedTo | ninguna tool invocada | `SpringAiOrchestrationAdapterTest.shouldReturnNullRoutedToWhenNoToolWasInvoked()` | COMPLIANT |
| Bypass de ruteo | bypass hacia doc-query | smoke real `X-Bypass-Routing: doc-query` | FAILING |
| Bypass de ruteo | bypass invalido | smoke real + `OrchestratorControllerTest.shouldReturnBadRequestWhenBypassHeaderIsInvalid()` | COMPLIANT |
| Fallo de downstream | downstream caido | `OrchestratorControllerTest.shouldReturnBadGatewayWhenDownstreamFails()` + smoke 502 | COMPLIANT |
| Proteccion de retry LLM | max attempts 1 | `application.properties` review | COMPLIANT |

## Findings

### CRITICAL

#### F1: Queda una tarea pendiente en `tasks.md`

**Dimension**: Tasks completeness  
**Location**: `.sdd/changes/orchestrator-agent/tasks.md:102`  
**Issue**: La tarea `8.1 Validacion end-to-end contra success criteria` sigue sin marcar como completada.  
**Expected**: Todas las tareas del apply deben estar `[x]` antes de archivar.  
**Observed**: `tasks.md` mantiene `- [ ] **8.1 Validacion end-to-end contra success criteria**`.  
**Why it matters**: SDD usa `tasks.md` como contrato de completitud. Un archive con tareas abiertas deja el cambio en estado ambiguo.  
**Suggested correction**: Ejecutar una tanda controlada de `sdd-apply`/validacion para completar 8.1 o ajustar formalmente el tasks si esa validacion se mueve a verify.  
**Action required**: completar o resolver explicitamente la tarea 8.1 y volver a correr `sdd-verify`.

---

#### F2: El bypass real a downstream devuelve 502

**Dimension**: Requirements coverage / Test execution  
**Location**: Requirement `Bypass de ruteo para desarrollo`; `agents/orchestrator-agent/src/main/java/com/genailab/orchestrator/infrastructure/adapter/ai/AgentTools.java:41`  
**Issue**: El endpoint levanta y el header invalido funciona, pero el bypass real hacia downstream no devuelve HTTP 200.  
**Expected**: Con `X-Bypass-Routing: doc-query`, el orchestrator debe saltear el LLM, invocar `doc-query-agent` y devolver HTTP 200 con `routedTo="doc-query-agent"`.  
**Observed**: `POST /api/v1/ask` con `X-Bypass-Routing: doc-query` devuelve HTTP 502. Logs: `Downstream agent failed: doc-query-agent request failed`. El bypass a `diagnosis` tambien devuelve 502; la llamada directa a `diagnosis-agent` desde host quedo colgada hasta timeout.  
**Why it matters**: El bypass era la herramienta para validar red/Docker sin gastar quota ni depender del LLM. Si falla, el principal mecanismo de smoke local no cumple el contrato.  
**Suggested correction**: Re-aplicar de forma controlada sobre integracion runtime: distinguir timeout vs 4xx/5xx real del downstream, registrar status/cause sin contenido sensible, y ajustar timeout/config o el smoke target hasta que un bypass disponible devuelva 200.  
**Action required**: hacer que al menos un bypass real a downstream disponible devuelva HTTP 200 y `routedTo` correcto, luego repetir smoke.

---

### WARNING

#### F3: Ruteo LLM local no fue validado end-to-end

**Dimension**: Requirements coverage  
**Location**: Requirement `Ruteo via LLM tool calling local`; `agents/orchestrator-agent/src/main/resources/application.properties:8`  
**Issue**: La implementacion configura `llama3`, pero no hay evidencia E2E de que `llama3` invoque tools correctamente via LiteLLM.  
**Expected**: Al menos un smoke sin bypass deberia mostrar tool invocation y `routedTo` real.  
**Observed**: No se ejecuto smoke sin bypass porque el verify ya bloqueo en bypass/downstream.  
**Why it matters**: El proposal reconoce que `llama3.2:3b` tuvo problemas de tool calling; sin smoke real, el riesgo principal sigue abierto.  
**Suggested correction**: Cuando F2 este resuelto, ejecutar `POST /api/v1/ask` sin bypass para doc-query/diagnosis/smart-search y verificar `routedTo`.

---

#### F4: `docker-compose.yaml` mantiene atributo `version` obsoleto

**Dimension**: Conventions / Docker hygiene  
**Location**: `docker-compose.yaml:1`  
**Issue**: Docker Compose emite warning: `the attribute version is obsolete`.  
**Expected**: Compose sin warnings evitables.  
**Observed**: El warning aparece en build, up, ps y logs.  
**Why it matters**: No rompe runtime, pero mete ruido en verify y puede tapar warnings reales.  
**Suggested correction**: Remover `version: "3.8"` en un cambio chico o durante el re-apply si el equipo lo acepta.

---

### SUGGESTION

#### F5: Mejorar diagnóstica de `DownstreamAgentException`

**Dimension**: Observability  
**Location**: `AgentTools.java` / `GlobalExceptionHandler.java`  
**Issue**: El log actual informa el agente fallido, pero no distingue timeout, conexion rechazada, 4xx o 5xx.  
**Suggested correction**: Loguear causa tecnica y status HTTP cuando exista, sin incluir pregunta, respuesta ni logContent.

## Verdict rationale

La implementacion compila, los tests unitarios/WebMvc pasan y la imagen Docker se construye. El cambio queda BLOCKED porque `tasks.md` todavia tiene una tarea abierta y el smoke E2E del bypass, que es un success criterion formal, devuelve 502 en vez de 200.

## Next steps

- No archivar este change.
- Volver a `sdd-apply` de forma controlada para resolver F1 y F2.
- Despues repetir `sdd-verify`, incluyendo smoke con bypass y al menos un smoke sin bypass si el downstream queda estable.
